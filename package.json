{
  "name": "tictactoe",
  "version": "0.1.0",
  "private": false,
  "homepage": "https://wnyao.github.io/tic-tac-toe/",
  "dependencies": {
    "braces": "^3.0.2",
    "gh-pages": "2.0.1",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.0.1"
  },
  "devDependencies": {
    "prettier": "1.18.2",
    "husky": "^3.0.0",
    "pretty-quick": "^1.10.0"
  },
  "scripts": {
    "predeploy": "$npm_execpath run build",
    "deploy": "$npm_execpath run predeploy && gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
